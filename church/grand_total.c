#include "main.h"

void grand()
{
	int Grand_Total;

	if ((tal_t + total_others + mflmelfme) - Grand_total == 0)
	{
		printf("correct total");
	}
	else
		printf("incorrect calculation of grand_total");
}
